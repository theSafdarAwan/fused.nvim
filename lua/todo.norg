** Add highlights for these plugins or improve previous
   - Lazy.nvim
   - nvim-tree.lua
   - diffview, neogit.

- [ ] add the markdown treesitter highlights
- [ ] find a way to load highlights for the plugin automatically if the plugins
  is loaded. Add `smart_load` key in setup tbl.
